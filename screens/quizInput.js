import {StyleSheet, Text, View} from 'react-native';

const QuizInput = ({navigation}) => {
  return (
    <View>
      <Text>Quiz Input</Text>
    </View>
  );
};

export default QuizInput;

const styles = StyleSheet.create({});
