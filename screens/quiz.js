import {StyleSheet, Text, View} from 'react-native';

const Quiz = ({navigation}) => {
  return (
    <View>
      <Text>Dashboard</Text>
    </View>
  );
};

export default Quiz;

const styles = StyleSheet.create({});
